// Placeholder — no FAQ section currently exists on the page (kept out of scope so the
// UI stays unchanged). Fill this in and add a <Faq /> section under src/components/artville
// if/when the site wants one.
export const faqs: { question: string; answer: string }[] = [];
